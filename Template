#include <bits/stdc++.h>
#define fastio ios::sync_with_stdio(0), cin.tie(0)
#define ll long long
using namespace std;

void solve(){
    int n; cin >> n;
    
}

#define TC 1
int32_t main(){
    fastio;
    ll tt;
    if (TC) cin >> tt;
    else tt = 1;
    while(tt--) solve();
    return 0;
}
